package ${targetPackage}.service;

import ${targetPackage}.persistence.entity.${domain};

/**
* @author ${author}
* @date ${date}
*/
public interface ${domain}Service {

${domain} find${domain}ById(Long ${domainVariable}Id);
}
